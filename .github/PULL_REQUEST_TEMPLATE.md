## Summary

<!-- What does this PR do and why? -->

## Testing

- [ ] `flutter analyze` passes clean
- [ ] `flutter test` passes
- [ ] Ran the app on affected platform(s) and manually verified the change

## Checklist

- [ ] Regenerated code if I touched freezed/injectable/l10n sources
      (`dart run build_runner build --delete-conflicting-outputs`, `flutter gen-l10n`)
- [ ] Updated `README.md` if setup/build steps changed
- [ ] PR is scoped to one change (no unrelated refactors/formatting)
